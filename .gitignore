# Cargo build artifacts and executables
/target/

# rustfmt backup files
**/*.rs.bk

# MSVC debugging information (Windows builds)
*.pdb

# Note: Cargo.lock is intentionally committed — this crate ships a binary
# (`TaskDeck`), so a locked, reproducible dependency set is wanted.

/taskdeck_data
/TODO.md
# macOS Finder metadata
.DS_Store

# A board writes its files beside itself, and a test that used "." as its
# data directory put one here. These hold calendar URLs, which are credentials.
/subscriptions.json
/read_at_startup.json
/archived.jsonl
/notepad_text.json
/subscribed_cache.json
